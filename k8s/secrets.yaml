apiVersion: v1
kind: Secret
metadata:
  name: convex-secrets
  namespace: pastebin
  labels:
    app.kubernetes.io/name: convex
    app.kubernetes.io/part-of: pastebin
type: Opaque
stringData:
  # Generate a secure admin key: openssl rand -base64 32
  # Replace this placeholder before deploying!
  admin-key: "REPLACE_WITH_SECURE_ADMIN_KEY"
