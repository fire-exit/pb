apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pastebin

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - convex-backend-pvc.yaml
  - convex-backend-deployment.yaml
  - convex-backend-service.yaml
  - convex-dashboard-deployment.yaml
  - convex-dashboard-service.yaml
  - nextjs-app-deployment.yaml
  - nextjs-app-service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize

# Uncomment to add image tags or patches
# images:
#   - name: your-registry/pastebin-app
#     newTag: v1.0.0
